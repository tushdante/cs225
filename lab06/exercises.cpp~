#include "exercises.h"

int sumDigits(int n)
{
	if (n==0) return 0;
	
	
    return((sumDigits(n/10)) + n%10); 
}

int triangle(int rows)
{
	if (rows==1) return 1;
	
	return (triangle(rows-1) + rows);
}
